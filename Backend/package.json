{
  "name": "taskly-backend",
  "version": "1.0.0",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "migrate": "psql $DATABASE_URL -f src/migrations/create_task_table.sql",
    "test": "jest --runInBand --detectOpenHandles --coverage",
    "test:unit": "jest --runInBand test/unit",
    "test:integration": "NODE_ENV=test jest --runInBand test/integration"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "pg": "^8.10.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "supertest": "^6.3.0",
    "nodemon": "^2.0.22"
  }
}